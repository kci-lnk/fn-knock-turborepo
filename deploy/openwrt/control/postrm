#!/bin/sh
set -e

[ -n "$IPKG_INSTROOT" ] && exit 0

rm -rf /tmp/fn-knock-updates 2>/dev/null || true
rm -f /tmp/luci-indexcache /tmp/luci-modulecache/* 2>/dev/null || true
[ -x /etc/init.d/rpcd ] && /etc/init.d/rpcd restart >/dev/null 2>&1 || true

exit 0
