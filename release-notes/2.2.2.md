# fn-knock 2.2.2

- 新增网络唤醒（WOL）管理，可发现局域网设备、检测在线状态、配置本地中继，并通过网关唤醒门户远程唤醒设备。
- 网络唤醒新增点灯科技（Blinker）和巴法云接入，可通过对应平台控制已配置的唤醒目标。
- 协议映射新增全局定时启用功能，可按服务器本地时间设置每日开放时段；定时关闭期间端口仍保持监听，但会拒绝新的 TCP 连接并丢弃 UDP 数据包，已有 TCP 会话不受影响。
- 系统设置新增日期时间显示模式，位于系统设置->功能，可在更易读的相对时间和完整日期时间之间切换。