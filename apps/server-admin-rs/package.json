{
  "name": "server-admin-rs",
  "private": true,
  "scripts": {
    "build": "cargo build --locked --release",
    "check-types": "cargo check --locked",
    "dev": "cargo run",
    "test": "cargo test --locked -- --test-threads=1"
  }
}
